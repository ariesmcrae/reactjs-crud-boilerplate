import {expect} from 'chai';
import jsdom from 'jsdom';
import fs from 'fs';

describe('Test index.js', () => {
    it('should pass', () => {
        expect(true).to.equal(true);
    });
});

describe(
    
);
